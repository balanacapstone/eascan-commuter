<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/lighter" /> <!-- Background color -->
    <stroke
        android:width="1dp"
        android:color="@color/black" /> <!-- Border color and width -->
    <corners android:radius="4dp" /> <!-- Rounded corners (optional) -->
</shape>